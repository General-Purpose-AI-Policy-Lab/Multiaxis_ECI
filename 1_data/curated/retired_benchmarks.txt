# Benchmarks retired from every fit: one name per line, '#' starts a comment.
# Matches the `benchmark` column of 1_data/processed/benchmarks_merged.csv exactly.
#
# Retirement is a measurement-validity rule, not a per-fit scope choice. These
# columns cannot carry a comparable loading row, so no fit may use them. The
# processed file still holds every benchmark; this list is applied at load time.
#
# FrontierMath v1, FrontierMath Tier 4 v1: superseded by v2. v2 shifted the
#   level about +0.20 on Tiers 1-3 with the ranking intact (paired r = 0.988),
#   so one column over both versions carries a bimodal difficulty. Tier 4 is
#   retired on the same rule: same problem-set replacement, same two-version
#   column, and v1 Tier 4 additionally caps at 0.60 answerable items where v2
#   does not.
# AlgoTune: the score is a 1 - 1/speedup transform, a scale no other benchmark
#   shares. Its difficulty and loading are not comparable to any other row.
# MindCube: 5 test-takers, four of them 2024-era open 7B models clustered
#   within 0.03 of the 0.3235 chance floor. A panel that thin, and that flat,
#   cannot identify a difficulty and a loading row.

FrontierMath v1
FrontierMath Tier 4 v1
AlgoTune
MindCube
