# Superseded renders

Figures the post no longer uses, kept for the record: the matplotlib twins of
figures whose Plotly versions became the deliverable, one crossover file under
a retired naming scheme, and two orphaned prior diagrams no script writes any
more. The matplotlib scripts still exist and write fresh copies one level up
if re-run.

Also here: the page renders of `docs/make_prior_graph.py`'s prior_graph.pdf
(`prior_graph-*.png`, source material for the diagrams that
`make_prior_diagrams.py` now draws directly) and the pre-envelope 80%-HDI
preview of the trend figure (`forecast_trend_plotly_hdi80.*`, redundant since
the main `forecast_trend_plotly` moved to 80% intervals).
