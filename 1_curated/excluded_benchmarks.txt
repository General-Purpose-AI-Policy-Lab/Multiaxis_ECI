# Benchmarks excluded from the index — one name per line, '#' starts a comment.
# Matches the `benchmark` column of 0_input/all_scores_flat.csv (the pipeline's names) exactly.
#
# Current list filters out "intentionally easy for humans" benchmarks —
# keeping them (even with the scores dropped) would bias both capability
# estimates and the positioning of other benchmarks.

ARC-AGI
ARC-AGI-2
HellaSwag
OpenBookQA
PIQA
SimpleBench
VPCT
WinoGrande
